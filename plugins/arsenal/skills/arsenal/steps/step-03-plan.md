# step 03 plan

Write the existing six-section roadmap schema in docs/roadmap/<slug>/roadmap.md. Keep optional diagrams empty rather than decorative. Review in the lead; an independent reviewer requires consent even if installed.

The current SKILL.md owns the workflow policy. User consent is required
before any agent, reviewer or advisor. No automatic validation sessions
for prose; no continuation after a user stop or completed result.

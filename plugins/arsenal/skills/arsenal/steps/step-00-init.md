# step 00 init

Resolve the requested outcome from the description and existing conversation.
Separate advice, documents and implementation; preserve permission limits.
Explicit install/uninstall flags use scripts/install_agents.py only for the
chosen harness, then stop. Resolve -r as an existing roadmap, not a new task.
Otherwise return to SKILL.md's adaptive clarification and routing rules.
Create no output directory until the selected skill requires an artifact.

The current SKILL.md owns the workflow policy. User consent is required
before any agent, reviewer or advisor. No automatic validation sessions
for prose; no continuation after a user stop or completed result.

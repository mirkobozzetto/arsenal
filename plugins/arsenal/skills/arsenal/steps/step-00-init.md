# step 00 init

Resolve idea or requested existing roadmap. Explicit install/uninstall flags use scripts/install_agents.py only for the chosen harness. No installation as a roadmap side effect. Load an adapter only when its capability is needed.

The current SKILL.md owns the workflow policy. User consent is required
before any agent, reviewer or advisor. No automatic validation sessions
for prose; no continuation after a user stop or completed result.
